"""Deprecated — session tracking is now owned by McpEventHandler.

This module is kept as a tombstone so any stale imports produce a clear error
rather than silently using old module-level state.
"""
